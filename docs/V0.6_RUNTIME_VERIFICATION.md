# LRBridge v0.6.0 Runtime Verification

- Lightroom Classic version: 15.3
- LRBridge branch: feature/v0.6-more-sdk-and-web-controller
- Verification status: Complete
- Code changes during runtime testing: None

Lightroom Classic 15.3 runtime verification is complete.

## Summary

- Runtime commands tested: **72**
- PASS: **71**
- FAIL: **1**
- UNVERIFIED: **0**
- Polling resilience: **PASS**
- Command processing after failures: **PASS**

An accepted and queued HTTP request confirms transport and queue acceptance only. It does not prove Lightroom executed the corresponding SDK operation.

## Complete runtime results

| Group | Command | Value | Result |
|---|---|---|---|
| 1 | `selection.operation` | `select_all` | **PASS** |
| 1 | `selection.operation` | `select_none` | **PASS** |
| 1 | `selection.operation` | `select_inverse` | **PASS** |
| 1 | `selection.operation` | `deselect_active` | **PASS** |
| 1 | `selection.operation` | `deselect_others` | **PASS** |
| 2 | `application.module` | `library` | **PASS** |
| 2 | `application.module` | `develop` | **PASS** |
| 2 | `application.module` | `map` | **PASS** |
| 2 | `application.module` | `book` | **PASS** |
| 2 | `application.module` | `slideshow` | **PASS** |
| 2 | `application.module` | `print` | **PASS** |
| 2 | `application.module` | `web` | **PASS** |
| 3 | `application.view` | `loupe` | **PASS** |
| 3 | `application.view` | `grid` | **PASS** |
| 3 | `application.view` | `compare` | **PASS** |
| 3 | `application.view` | `survey` | **PASS** |
| 3 | `application.view` | `people` | **PASS** |
| 4 | `application.view` | `develop_loupe` | **PASS** |
| 4 | `application.view` | `develop_before_after_horiz` | **PASS** |
| 4 | `application.view` | `develop_before_after_vert` | **PASS** |
| 4 | `application.view` | `develop_before` | **PASS** |
| 4 | `application.view` | `develop_reference_horiz` | **PASS** |
| 4 | `application.view` | `develop_reference_vert` | **PASS** |
| 5 | `application.action` | `toggle_zoom` | **PASS** |
| 5 | `application.action` | `zoom_in` | **PASS** |
| 5 | `application.action` | `zoom_out` | **PASS** |
| 5 | `application.action` | `zoom_100` | **PASS** |
| 5 | `application.action` | `fullscreen_preview` | **PASS** |
| 5 | `application.action` | `fullscreen_hide_panels` | **PASS** |
| 5 | `application.action` | `next_screen_mode` | **PASS** |
| 5 | `application.action` | `cycle_loupe_info` | **FAIL** |
| 6 | `application.action` | `toggle_secondary_display` | **PASS** |
| 6 | `application.action` | `toggle_secondary_fullscreen` | **PASS** |
| 6 | `application.secondary_view` | `grid` | **PASS** |
| 6 | `application.secondary_view` | `loupe` | **PASS** |
| 6 | `application.secondary_view` | `live_loupe` | **PASS** |
| 6 | `application.secondary_view` | `locked_loupe` | **PASS** |
| 6 | `application.secondary_view` | `compare` | **PASS** |
| 6 | `application.secondary_view` | `survey` | **PASS** |
| 6 | `application.secondary_view` | `slideshow` | **PASS** |
| 7 | `selection.navigate` | `first` | **PASS** |
| 7 | `selection.navigate` | `previous` | **PASS** |
| 7 | `selection.navigate` | `next` | **PASS** |
| 7 | `selection.navigate` | `last` | **PASS** |
| 7 | `selection.flag` | `pick` | **PASS** |
| 7 | `selection.flag` | `reject` | **PASS** |
| 7 | `selection.flag` | `none` | **PASS** |
| 7 | `selection.rating.set` | `0` | **PASS** |
| 7 | `selection.rating.set` | `3` | **PASS** |
| 7 | `selection.rating.set` | `5` | **PASS** |
| 7 | `selection.rating.adjust` | `increase` | **PASS** |
| 7 | `selection.rating.adjust` | `decrease` | **PASS** |
| 7 | `selection.label.set` | `red` | **PASS** |
| 7 | `selection.label.set` | `none` | **PASS** |
| 7 | `selection.label.toggle` | `red` | **PASS** |
| 8 | `develop.action` | `resetCrop` | **PASS** |
| 8 | `develop.action` | `resetTransforms` | **PASS** |
| 8 | `develop.action` | `setAutoTone` | **PASS** |
| 8 | `develop.action` | `setAutoWhiteBalance` | **PASS** |
| 8 | `develop.action` | `resetSpotRemoval` | **PASS** |
| 8 | `develop.action` | `resetRedeye` | **PASS** |
| 8 | `develop.action` | `selectCropTool` | **PASS** |
| 8 | `develop.action` | `selectHealingTool` | **PASS** |
| 8 | `develop.action` | `selectRedEyeTool` | **PASS** |
| 8 | `develop.action` | `selectUprightTool` | **PASS** |
| 8 | `develop.action` | `selectMaskingTool` | **PASS** |
| 8 | `develop.action` | `resetAllDevelopAdjustments` | **PASS** |
| 9 | `develop.adjust` | `Exposure / amount=1` | **PASS** |
| 10 | `photo.rotate` | `left` | **PASS** |
| 10 | `photo.rotate` | `right` | **PASS** |
| 10 | `selection.extend` | `right / amount=1` | **PASS** |
| 10 | `selection.extend` | `left / amount=1` | **PASS** |

## Controlled-retest notes

- The authoritative `select_all` result is **PASS**. The initial attempt occurred before the development plugin was reloaded, while Lightroom had cached the old Lua modules.
- The authoritative results for the original nine Lightroom 15.3 no-ops are the controlled re-tests shown above: eight now **PASS**, while `application.action / cycle_loupe_info` remains **FAIL**.
- The authoritative `toggle_secondary_fullscreen` result is **PASS**. The controlled test confirmed that it changed the Secondary Display from fullscreen to windowed.
- The authoritative `resetAllDevelopAdjustments` result is **PASS**. Lightroom was completely restarted, and fresh `AutoStartPolling` initialization occurred after the updated `Driver.lua` modification time. Exactly one command was admitted; the queue transitioned `0 → 1 → 0`; enqueue and dequeue deltas were both 1; heartbeat and polling remained active; Lightroom remained in Develop; and the active photo visibly reset Exposure, Contrast, White Balance, crop, and all other Develop adjustments.
- The earlier apparent `resetAllDevelopAdjustments` failure was **INCONCLUSIVE**, not a command failure. Lightroom was still running a cached `Driver.lua` loaded before the action existed. Unknown actions can currently be dequeued and return `false` silently without producing a result payload. A complete Lightroom restart is required after Lua plug-in source changes before controlled runtime testing.
- Lightroom was completely restarted after `Commands.lua`, `Selection.lua`, and `Photo.lua` changed. Fresh `AutoStartPolling: silent polling started` and `AutoStartPolling: polling loop started` entries occurred after all three file modification times before testing the four new operations.
- Each `photo.rotate` and `selection.extend` test admitted exactly one valid command, observed queue completion `0 → 1 → 0`, recorded enqueue and dequeue deltas of 1, and retained advancing heartbeat and active polling.
- `photo.rotate / left` visibly rotated the active photo 90 degrees counter-clockwise; `photo.rotate / right` visibly rotated it 90 degrees clockwise.
- `selection.extend / right / amount=1` added exactly one adjacent photo to the right while retaining the original selection; `selection.extend / left / amount=1` did the same to the left.
- Before the first rotation test, a request using `/api/command` was mistakenly sent to backend port 17891. It returned HTTP 404 and was never admitted or enqueued, so it was a routing mistake rather than a Lightroom command failure. In the development setup, `/api/command` is exposed by the Web Controller proxy on port 17892; direct backend command routes are on port 17891.

## Confirmed failures

The sole authoritative failure is:

1. `application.action / cycle_loupe_info`

Its HTTP request was accepted and consumed, but Lightroom Classic 15.3 produced no visible result during the controlled compatible-state retest.

## Group 11 — resilience

| Verification | Result |
|---|---|
| Known-working `application.module / develop` after earlier failures | **PASS** |
| `/status` polling resilience | **PASS** |
| `/context` polling resilience | **PASS** |

The earlier failed and context-incompatible SDK commands did not stop plugin polling or command processing. The final Develop command visibly worked, the queue returned to zero, the context counter advanced, and the heartbeat remained active.

No HTTP transport failure, queue failure, plugin stoppage, or explicit Lightroom/plugin error payload was observed. The sole remaining failure manifested as a silent visible no-op.

## Required post-verification implementation work

1. Replace the startup keyboard-shortcut/focus mechanism that forces Library with the verified SDK-native `application.module / library` command.

   - Trigger it once after plugin heartbeat/polling becomes active.
   - Do not force Library repeatedly.

2. Investigate the remaining documented-but-nonfunctional Lightroom Classic 15.3 SDK operation, `application.action / cycle_loupe_info`.

   - Remove it, mark it unsupported, or retain it with an explicit warning based on the investigation.
   - Do not add keyboard-shortcut fallbacks.

3. Improve command-result diagnostics so accepted/queued commands can be distinguished from silent Lightroom SDK no-ops.

4. Update the Web Controller, validation, help, tests, README, and changelog to reflect only verified behavior.

5. Preserve the demonstrated polling resilience.

## Repository state during runtime verification

Runtime testing did not modify implementation files. The working tree already contained the uncommitted v0.6 implementation changes under test. No commits or pushes were made during runtime verification.
